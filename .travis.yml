language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip3 install -r requirements.txt
# command to run tests
script:
  - pytest # or py.test for Python versions 3.5 and below
  - docker build -t biocreative_vi_relation_extraction:ae .
